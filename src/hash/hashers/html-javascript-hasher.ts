import { HTMLHasher } from '../generics/html-hasher';

// Use all javavscript variables/methods in HTML as features

export class HTMLJavascriptHasher extends HTMLHasher {
  constructor() {
    super();
  }
}